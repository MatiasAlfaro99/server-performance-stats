Server Performance Stats (server-stats.sh)Proyecto originalRequirements:You are required to write a script server-stats.sh that can analyse basic server performance stats. You should be able to run the script on any Linux server and it should give you the following stats:Total CPU usageTotal memory usage (Free vs Used including percentage)Total disk usage (Free vs Used including percentage)Top 5 processes by CPU usageTop 5 processes by memory usageStretch goal: Feel free to optionally add more stats such as os version, uptime, load average, logged in users, failed login attempts etc.El problema exige la construcción de un script capaz de extraer, calcular y formatear métricas de rendimiento del sistema operativo. Este script debe reducir al mínimo la instalación de una dependencia externa.Instalación y UsoOtorga permisos de ejecución al archivo:chmod +x server-stats.sh
Ejecuta el script de manera estándar:./server-stats.sh
Desglose Técnico de ComandosInstrucción CompletaDesglose Técnico de ParámetrosPropósito en el Scripttop -bn1-b (Batch mode): Modo por lotes para salida a consola.-n 1: Detiene el comando tras un solo ciclo.Capturar una fotografía estática de la CPU para procesarla con awk como texto plano.free -m-m (Megabytes): Muestra la asignación de memoria en megabytes.Estandarizar la unidad de medida para calcular porcentajes de uso de RAM.df -h /-h (Human-readable): Valores en GB/MB./ (Target): Apunta a la partición raíz.Extraer tamaño y espacio disponible del sistema operativo ignorando unidades virtuales.ps -eo pid,comm,%cpu-e (Every): Todos los procesos.-o (Output): Columnas de PID, binario y % de CPU.Filtrar la salida para obtener solo los datos necesarios para el Top 5.--sort=-%cpu--sort: Ordenamiento de salida.- (Menos): Orden descendente.%cpu: Criterio de orden.Colocar los procesos con mayor carga de CPU en la parte superior.head -n 6-n 6: Corta el flujo en la sexta línea.Mostrar cabecera y los 5 procesos principales.uptime -p-p (Pretty): Formato amigable del tiempo de actividad.Evitar conversiones matemáticas manuales desde /proc/uptime.wc -l-l (Lines): Cuenta saltos de línea.Transformar listas de usuarios o intentos fallidos en valores numéricos.
